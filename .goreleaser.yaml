version: 2

before:
  hooks:
    - go mod tidy

builds:
  - id: slackcli
    main: ./cmd/slackcli
    binary: slackcli
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X github.com/jackchuka/slackcli/internal/version.Version={{.Version}}
      - -X github.com/jackchuka/slackcli/internal/version.Commit={{.Commit}}
      - -X github.com/jackchuka/slackcli/internal/version.BuildDate={{.Date}}

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"
      - "^style:"
      - "^refactor:"
